### Q1: build.gradle.kts ফাইলের কাজ কী? এটি কেন প্রয়োজন? 
- Ans: এটি হলো অ্যান্ড্রয়েড প্রজেক্টের Build Configuration ফাইল। অ্যাপের ভার্সন কন্ট্রোল করা, নতুন লাইব্রেরি (Dependencies) যুক্ত করা এবং অ্যাপটি কীভাবে বিল্ড হবে (যেমন: APK বা App Bundle) তা এই ফাইল দিয়ে নিয়ন্ত্রণ করা হয়। এটি অনেকটা ফ্লাটারের pubspec.yaml এর মতো কাজ করে।

### Q2: applicationId এবং namespace এর মধ্যে পার্থক্য কী? 
- Ans: 
- applicationId: এটি প্লে-স্টোরে আপনার অ্যাপের ইউনিক পরিচয়। একবার অ্যাপ পাবলিশ করলে এটি আর পরিবর্তন করা যায় না।
- namespace: এটি আপনার কোডের ভেতরের ইন্টারনাল প্যাকেজ নেম, যা R.java ফাইল এবং অন্যান্য রিসোর্স জেনারেট করতে ব্যবহৃত হয়।

### Q3: minSdk এবং targetSdk বলতে কী বোঝায়? 
Ans: 
- minSdk: এটি নির্দেশ করে সর্বনিম্ন কত ভার্সনের অ্যান্ড্রয়েড ফোনে আপনার অ্যাপটি চলবে। (যেমন: minSdk = 24 মানে অ্যান্ড্রয়েড ৭.০ এর নিচের ফোনে অ্যাপটি চলবে না)।
- targetSdk: এটি নির্দেশ করে আপনি কোন অ্যান্ড্রয়েড ভার্সন পর্যন্ত আপনার অ্যাপটিকে লেটেস্ট ফিচারের জন্য টেস্ট এবং অপ্টিমাইজ করেছেন।

### Q4: নতুন কোনো লাইব্রেরি বা প্যাকেজ কীভাবে যোগ করতে হয়? 
- Ans: ফাইলের একদম নিচে থাকা dependencies { ... } ব্লকের ভেতরে implementation কিওয়ার্ড ব্যবহার করে নতুন লাইব্রেরি যোগ করতে হয়। পরিবর্তন করার পর অবশ্যই অ্যান্ড্রয়েড স্টুডিওর "Sync Now" বাটনে ক্লিক করতে হয়।

### Q5: buildFeatures { compose = true } কেন ব্যবহার করা হয়েছে? 
- Ans: এটি নির্দেশ করে যে এই প্রজেক্টে Jetpack Compose (অ্যান্ড্রয়েডের আধুনিক ডিক্লারেটিভ UI ফ্রেমওয়ার্ক) ব্যবহার করা হচ্ছে। এটি ছাড়া অ্যাপে কম্পোজ ফাংশনগুলো কাজ করবে না।

### Q6: isMinifyEnabled = false এর কাজ কী? 
- Ans: এটি রিলিজ মোডে কোডকে ছোট (Shrink) এবং অপ্টিমাইজ (Obfuscate) করার জন্য ব্যবহৃত হয়। রিলিজের সময় এটি true করে দিলে অ্যাপের সাইজ কমে যায় এবং কোড রিভার্স-ইঞ্জিনিয়ারিং করা কঠিন হয়।

### Q7: অ্যান্ড্রয়েড লাইব্রেরিগুলো কোথা থেকে ডাউনলোড হয়? 
- Ans: এই লাইব্রেরিগুলো মূলত Google Maven Repository এবং Maven Central থেকে ডাউনলোড হয়। ফ্লাটারে যেমন pub.dev থাকে, নেটিভ অ্যান্ড্রয়েডের জন্য এই রিপোজিটরিগুলো একই কাজ করে।

### Q8: implementation এবং debugImplementation এর মধ্যে পার্থক্য কী? 
- Ans: 
- implementation: এই লাইব্রেরিগুলো অ্যাপের মূল কোডে যুক্ত হয় এবং রিলিজ করা অ্যাপে (APK) এগুলো থাকে।
- debugImplementation: এগুলো শুধুমাত্র অ্যাপ ডেভেলপারদের সুবিধার্থে (যেমন: UI প্রিভিউ দেখা) ব্যবহৃত হয়। অ্যাপ যখন রিলিজ করা হয়, তখন এই লাইব্রেরিগুলো বাদ দেওয়া হয় যাতে অ্যাপের সাইজ ছোট থাকে।

### Q9: গ্র্যাডল (Gradle) কী? 
- Ans: গ্র্যাডল হলো একটি বিল্ড অটোমেশন টুল। এটি আপনার লেখা কোড, ছবি এবং লাইব্রেরিগুলোকে একসাথে করে একটি পূর্ণাঙ্গ অ্যান্ড্রয়েড অ্যাপ (APK) তৈরি করে। এটি মূলত প্রজেক্টের "রাজমিস্ত্রি" হিসেবে কাজ করে।

### Q10: গ্র্যাডল-এর প্রধান কাজ কী? Ans: এর প্রধান কাজ ৩টি:
- ১. ইন্টারনেট থেকে প্রয়োজনীয় লাইব্রেরি (Dependencies) ডাউনলোড করা।
- ২. কটলিন কোডকে মেশিন কোডে রূপান্তর (Compile) করা।
- ৩. অ্যাপের ফাইলগুলোকে গুছিয়ে ইন্সটল করার যোগ্য করা।