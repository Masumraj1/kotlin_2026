
### Q1: অ্যান্ড্রয়েড অ্যাপের নাম (Display Name) কীভাবে পরিবর্তন করতে হয়?
- Ans: res > values > strings.xml ফাইলে গিয়ে <string name="app_name"> ট্যাগের ভেতরে নতুন নাম লিখে দিলেই অ্যাপের নাম পরিবর্তন হয়ে যায়।

### Q2: সরাসরি ম্যানিফেস্ট ফাইলে নাম পরিবর্তন না করে strings.xml ব্যবহার করা হয় কেন?
- Ans: এটি একটি "Best Practice"। এর ফলে অ্যাপে সহজেই একাধিক ভাষা (যেমন: বাংলা ও ইংরেজি) যুক্ত করা যায় এবং এক জায়গা থেকেই পুরো অ্যাপের নাম নিয়ন্ত্রণ করা যায়।

### Q3: AndroidManifest.xml দেখে কীভাবে বুঝবো কোন স্ক্রিনটি আগে ওপেন হবে?
- Ans: যে <activity> ট্যাগের ভেতরে নিচের intent-filter কোডটুকু থাকবে, সেই স্ক্রিনটিই অ্যাপ ওপেন করলে সবার আগে আসবে:

```bash
<intent-filter>
    <action android:name="android.intent.action.MAIN" />
    <category android:name="android.intent.category.LAUNCHER" />
</intent-filter>
```
### Q4: অ্যাপে ইন্টারনেট বা ক্যামেরা পারমিশন কীভাবে নিতে হয়?
- Ans: <application> ট্যাগের ঠিক উপরে <uses-permission android:name="android.permission.INTERNET" /> অথবা ক্যামেরার জন্য প্রয়োজনীয় পারমিশন ট্যাগ যোগ করে পারমিশন নিতে হয়।

### Q5: নতুন কোনো স্ক্রিন (Activity) তৈরি করলে কি ম্যানিফেস্টে লিখতে হয়?
- Ans: হ্যাঁ, অ্যাপের প্রতিটি নতুন অ্যাক্টিভিটিকে ম্যানিফেস্টের ভেতরে <activity android:name=".YourActivityName" /> ট্যাগের মাধ্যমে রেজিস্টার করতে হয়। না করলে অ্যাপ ক্রাশ করবে।

### Q6: অ্যাপের থিম (Theme) বা কালার স্টাইল কোথা থেকে কন্ট্রোল করা হয়?
- Ans: ম্যানিফেস্টের android:theme এট্রিবিউটের মাধ্যমে পুরো অ্যাপের গ্লোবাল থিম সেট করা হয়। এটি সাধারণত @style/Theme.YourProjectName ফাইলকে রেফার করে।

### Q7: AndroidManifest.xml ফাইলের মূল কাজ কী?
- Ans: এটি একটি অ্যাপের "পরিচয়পত্র"। অ্যাপের নাম, প্যাকেজ নেম, কোন স্ক্রিন আগে ওপেন হবে এবং অ্যাপের কী কী পারমিশন লাগবে—সবই এখানে ডিফাইন করা থাকে।

### Q8: অ্যান্ড্রয়েড অ্যাপের আইকন পরিবর্তন করার প্রফেশনাল উপায় কী? 
- Ans: অ্যান্ড্রয়েড স্টুডিওর "Image Asset" টুল ব্যবহার করা। এর মাধ্যমে একবার আইকন দিলে এটি সব সাইজের ফোনের জন্য অটোমেটিক আইকন তৈরি করে নেয়।

### Q9: Image Asset টুলটি কীভাবে ওপেন করতে হয়? 
- Ans: 1. app ফোল্ডারের ওপর রাইট ক্লিক করুন। 2. New > Image Asset সিলেক্ট করুন। 3. Path থেকে আপনার লোগো বা ইমেজটি সিলেক্ট করে Finish দিন।

### Q10: @mipmap ফোল্ডার কেন ব্যবহার করা হয়, @drawable কেন নয়? 
- Ans: আইকনের জন্য @mipmap ব্যবহার করা হয় কারণ এটি বিভিন্ন স্ক্রিন রেজোলিউশনের (যেমন: hdpi, xxhdpi) ফোনে আইকনটিকে ঘোলা না করে পরিষ্কার দেখাতে সাহায্য করে।

### Q11: android:supportsRtl="true" এর কাজ কী? 
- Ans: এটি অ্যান্ড্রয়েড সিস্টেমকে বলে যে, আপনার অ্যাপটি ডান-থেকে-বাম (Right-to-Left) দিকে লেখা হয় এমন ভাষা (যেমন: আরবি, ফার্সি বা হিব্রু) সাপোর্ট করবে।

### Q12:res/drawable ফোল্ডারটির কাজ কী? 
- Ans: এই ফোল্ডারে অ্যাপের সব ভিজ্যুয়াল এলিমেন্ট বা গ্রাফিক্স ফাইল রাখা হয়। যেমন— ইমেজ (PNG, JPEG), ভেক্টর আইকন (SVG/XML), এবং বাটনের শেপ বা ব্যাকগ্রাউন্ড ডিজাইন।

### Q13: ইমেজ ব্যবহারের ক্ষেত্রে drawable আর mipmap-এর পার্থক্য কী?
- Ans: - drawable: এখানে অ্যাপের ভেতরের সব ছবি এবং আইকন রাখা হয়।
- mipmap: এখানে শুধুমাত্র অ্যাপের লঞ্চার আইকন (যেটি ফোনের হোম স্ক্রিনে দেখা যায়) রাখা হয়। 
